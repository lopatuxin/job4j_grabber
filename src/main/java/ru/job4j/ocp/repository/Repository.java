package ru.job4j.ocp.repository;

public class Repository {
    public void Save(AbstractEntity entity) {
        if (entity.equals(AccountEntity.class)) {

        }
        if (entity.equals(RoleEntity.class)){

        }
    }
}
